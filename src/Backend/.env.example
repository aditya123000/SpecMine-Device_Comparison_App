PORT=8000
CORS_ORIGIN=http://localhost:5173,https://your-project.vercel.app,https://*.vercel.app
JWT_SECRET=replace-with-a-long-random-secret

# Preferred for hosted databases like Supabase / Vercel Postgres:
# DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
#
# Optional local Postgres setup if you are not using DATABASE_URL:
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=compare_devices
# PGUSER=postgres
PGPASSWORD=your_postgres_password

# Set to true for Supabase / most hosted Postgres providers
PG_SSL=true

# Auto-import devices from Data/db.json when the devices table is empty
AUTO_SEED_DB=true
